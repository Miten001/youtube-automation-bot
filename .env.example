# ---- YouTube (OAuth 2.0 Desktop app) ----
# Path to the OAuth client secret JSON downloaded from Google Cloud Console.
YT_CLIENT_SECRET_FILE=client_secret.json
# Where the cached OAuth token will be stored after first login.
YT_TOKEN_FILE=token.json
